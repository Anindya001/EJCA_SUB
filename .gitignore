# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.spl
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/b*.log
# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
paper/*.abs

# Convenience bundle of paper/ + github_repro_EJCA/, rebuilt on demand.
# The Overleaf submission package (EJCA_overleaf_R2.zip) IS tracked, since it
# carries the CAS class files and is the artifact actually submitted.
EJCA_full_R2.zip
